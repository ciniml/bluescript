{
  "name": "@bscript/web",
  "version": "2.1.0",
  "private": true,
  "description": "BlueScript in the browser: WebAssembly toolchain + Web Bluetooth + esptool-js",
  "scripts": {
    "build": "node build.mjs",
    "serve": "node build.mjs --serve"
  },
  "devDependencies": {
    "buffer": "^6.0.3",
    "esbuild": "^0.24.0"
  },
  "dependencies": {
    "esptool-js": "^0.5.0"
  }
}
