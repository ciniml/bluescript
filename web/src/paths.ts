// Paths inside the in-memory filesystem shared by the browser modules.
export const BUNDLE_DIR = '/bundle';
export const PROJECT_DIR = '/project';
export const PROJECT_NAME = 'app';
export const PACKAGES_DIR = `${PROJECT_DIR}/packages`;
